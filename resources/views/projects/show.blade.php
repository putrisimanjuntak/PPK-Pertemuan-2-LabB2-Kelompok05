@extends('layouts.app') @section('content')<div class="card"><h1>{{$project->name}}</h1><p>{{$project->description}}</p><a class="btn" href="{{route('tasks.create',$project)}}">+ Tugas</a> <a class="btn" href="{{route('projects.edit',$project)}}">Edit</a><form class="inline" method="POST" action="{{route('projects.destroy',$project)}}">@csrf @method('DELETE')<button class="danger" onclick="return confirm('Hapus project?')">Hapus</button></form></div><div class="card"><h2>Tugas</h2>@forelse($project->tasks as $t)<p><a href="{{route('tasks.show',$t)}}">{{$t->title}}</a> <span class="badge">{{$t->status}}</span></p>@empty<p>Belum ada tugas.</p>@endforelse</div>@endsection
