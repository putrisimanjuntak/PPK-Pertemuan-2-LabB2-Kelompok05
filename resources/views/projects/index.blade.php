@extends('layouts.app') @section('content')<div style="display:flex;justify-content:space-between"><h1>Project</h1><a class="btn" href="{{route('projects.create')}}">+ Project Baru</a></div><div class="grid">@forelse($projects as $p)<div class="card"><h2>{{$p->name}}</h2><p class="muted">{{$p->description}}</p><p>{{$p->tasks_count}} tugas</p><a class="btn" href="{{route('projects.show',$p)}}">Buka</a></div>@empty<div class="card">Belum ada project.</div>@endforelse</div>@endsection
