@extends('layouts.app') @section('content')<div class="card"><h1>{{$project->exists?'Edit':'Project Baru'}}</h1><form method="POST" action="{{$project->exists?route('projects.update',$project):route('projects.store')}}">@csrf @if($project->exists)@method('PUT')@endif<input name="name" placeholder="Nama project" value="{{old('name',$project->name)}}" required><textarea name="description" placeholder="Deskripsi">{{old('description',$project->description)}}</textarea><button>Simpan</button></form></div>@endsection
