@extends('layouts.app') @section('content')<div class="card"><h1>{{$user->exists?'Edit':'Tambah'}} User</h1><form method="POST" action="{{$user->exists?route('admin.users.update',$user):route('admin.users.store')}}">@csrf @if($user->exists)@method('PUT')@endif<input name="name" placeholder="Nama" value="{{old('name',$user->name)}}" required><input name="email" type="email" placeholder="Email" value="{{old('email',$user->email)}}" required><input name="password" type="password" placeholder="{{$user->exists?'Kosongkan jika tidak diubah':'Password'}}" @if(!$user->exists)required @endif><input name="password_confirmation" type="password" placeholder="Konfirmasi password"><select name="role"><option value="user" @selected(old('role',$user->role)=='user')>user</option><option value="admin" @selected(old('role',$user->role)=='admin')>admin</option></select><button>Simpan</button></form></div>@endsection
